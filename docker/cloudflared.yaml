services:
  tunnel:
    container_name: cloudflared
    image: cloudflare/cloudflared
    restart: always
    command: tunnel run
    environment:
      - TUNNEL_TOKEN= #key
    networks:
      - proxy